<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/backgroundBlue"
    android:orientation="vertical"
    tools:context="edu.temple.keybeamlab.MainActivity">

    <TextView
        android:id="@+id/sendTextView"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:textSize="20dp"
        android:text="Text to Send:"/>

    <EditText
        android:id="@+id/textToSend"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:textSize="20dp"
        android:background="@color/editTextWhite"
        ></EditText>

    <TextView
        android:id="@+id/receiveEncryptedTextView"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:textSize="20dp"
        android:text="Encrypted Text Received:"/>

    <EditText
        android:id="@+id/textEncryptedReceived"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:textSize="20dp"
        android:background="@color/editTextWhite"
        ></EditText>

    <TextView
        android:id="@+id/receiveTextView"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:textSize="20dp"
        android:text="Decrypted Text Received:"/>

    <EditText
        android:id="@+id/textReceived"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:textSize="20dp"
        android:background="@color/editTextWhite"
        ></EditText>
</LinearLayout>
